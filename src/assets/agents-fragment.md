The {{brand}} plugin gives this project an MCP server (`{{name}} mcp`) with
its tools. Use them when the task needs the plugin's functionality. Check
configuration with `npx {{name}} config get` and run `npx {{name}} doctor`
when something is not working.

