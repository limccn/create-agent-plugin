{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "my-agent-plugin": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "my-agent-plugin",
        "mcp"
      ]
    }
  }
}
